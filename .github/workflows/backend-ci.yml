name: Backend CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      mongo:
        image: mongo:latest
        ports:
          - 27017:27017
        options: --bind_ip_all

    steps:
      # Step 1: Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Set up Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '16'
          cache: npm

      # Step 3: Install backend dependencies
      - name: Install backend dependencies
        working-directory: ./backend  # Ensure correct path
        run: npm install

      # Step 4: Set up environment variables for the backend
      - name: Set up environment variables
        run: |
          echo "MONGO_URI=mongodb://localhost:27017/testdb" >> ./backend/.env
          echo "JWT_SECRET=supersecretjwtkey12345" >> ./backend/.env

      # Step 5: Run backend tests to verify MongoDB connection
      - name: Run backend tests
        working-directory: ./backend  # Ensure correct path
        run: npm test
